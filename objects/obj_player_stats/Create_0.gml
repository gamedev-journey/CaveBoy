hp = 3;
maxHp = 3;
sapphires = 0;

display_set_gui_size(camera_get_view_width(view_camera[0]), camera_get_view_height(view_camera[0]));

audio_play_sound(msc_title, 10, false);

window_set_cursor(cr_none);

randomize();